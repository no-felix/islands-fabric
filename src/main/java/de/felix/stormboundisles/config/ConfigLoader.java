package de.felix.stormboundisles.config;

import java.nio.file.Path;

/**
 * Loads and saves mod configuration (teams, zones, disasters, buffs, points).
 */
public class ConfigLoader {
	// TODO: Implement loading/saving from YAML/JSON
	public void loadConfig(Path configPath) {
		// Load config and apply to managers
	}

	public void saveConfig(Path configPath) {
		// Save current state to config
	}
}
